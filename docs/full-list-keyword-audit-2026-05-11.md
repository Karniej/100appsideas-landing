# Full List Keyword Audit - 2026-05-11

Source: Astro MCP temporary app `100AppIdeas All 76 Keyword Audit` (`appId` 115).

Audit scope:

- 76 app ideas
- 486 unique primary and target keywords
- US App Store, iPhone
- Active ideas must have at least one tracked keyword at popularity > 20 and difficulty < 45

Result:

- 47 active unbuilt ideas remain
- 1 shipped idea remains
- 28 ideas are now dead / monitor-only
- 0 active ideas are missing a qualifying tracked keyword after the audit

New MAKESENSE? ideas added:

- Seating Chart Planner - `seating chart`, pop 23 / diff 15, rank #25
- Concrete Calculator - `concrete calculator`, pop 36 / diff 23, rank #39
- Bunco Scorekeeper - `bunco`, pop 22 / diff 41, rank #45
- Square Footage Calculator - `square footage calculator`, pop 24 / diff 42, rank #43
- Headache Tracker - `headache tracker`, pop 30 / diff 39, rank #46
- Jump Rope Counter - added from MAKESENSE?, then killed by consolidated audit
- Knot Tying Guide - added from MAKESENSE?, then killed by consolidated audit

Ruthless audit demotions:

- Cat Pain Score - dead; `feline grimace scale` now pop 9 / diff 19
- Game Aim Converter - dead; `game aim converter` now pop 14 / diff 13
- Deck of Cards - dead; `deck of cards` now pop 6 / diff 13
- Loom Knitting Counter - dead; `loom` now pop 54 / diff 46
- Jump Rope Counter - dead; `jump rope` now pop 7 / diff 21 in the consolidated app
- Funko Pop Tracker - dead; `funko pop` now pop 56 / diff 46 and IP-heavy
- Line Memorizer - dead; `line learner` now pop 8 / diff 37
- Mole Checker - dead; `mole checker` now pop 7 / diff 19
- Reptile Care Guide - dead; `reptile` now pop 9 / diff 19
- Volleyball Scorer - dead; `volleyball score` now pop 18 / diff 23
- Auto Clicker - dead; `auto clicker` now pop 68 / diff 50 and iOS capability risk is high
- Volume Booster - dead; `volume booster` now pop 60 / diff 49
- Light Meter - dead; `light meter` now pop 54 / diff 46
- Cribbage Scoreboard - dead; `cribbage` now pop 53 / diff 57
- Liar's Dice Scorekeeper - dead; `liars dice` is pop 20 / diff 13, failing the strict pop > 20 cutoff
- HVAC Calculator - dead; `hvac calculator` is pop 20 / diff 19, failing the strict pop > 20 cutoff
- Knot Tying Guide - dead; `knot tying` is pop 26 / diff 45, failing the strict diff < 45 cutoff

Kept but downgraded:

- Rain Gauge Tracker - weak; still qualifies at pop 34 / diff 42
- Cosplay Planner - weak; still qualifies at pop 30 / diff 36
- Elf Alert - moderate; still qualifies at pop 59 / diff 42
- Image Converter - weak; valid angle changed to `heic to jpg`, pop 21 / diff 44
- Concrete Calculator - weak; pop 36 / diff 23, but top incumbent has 24k ratings

Kept with ASO correction:

- First Aid & CPR Guide - primary keyword changed to `resuscitation skills`, pop 27 / diff 15. `first aid` itself dropped to pop 7.

Rejected MAKESENSE? keywords:

- `scale for grams` / `digital scale` - misleading iPhone scale intent
- `sobrief` - brand search
- `fish verify` - brand search and already overlaps Fishify / fish rules ideas
- `skyjo` - IP-heavy game keyword
- `height predictor` - crowded with dubious growth claims
- Existing covered keywords: `haiku`, `psat score`, `qsr inventory`, `animal scat identifier`, `birthday candle app`, `first aid`, `roof pitch gauge`, `hvac`, `jewelry identifier`, and others already represented in the list

Rule after audit:

If an idea is active, at least one primary or target keyword in the consolidated Astro app must currently qualify under the cutoff. If not, it moves to `DEAD` even if the app concept still sounds plausible.
